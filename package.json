{
    "private": true,
    "name": "adc-platform-monorepo",
    "scripts": {
        "build": "echo 'Debugging Vercel Environment...' && ls -R apps && echo 'Building Web App...' && cd apps/frontend && npm install && npm run build"
    },
    "engines": {
        "node": "20.x"
    },
    "devDependencies": {
        "next": "14.2.3"
    }
}