```json
{
    "private": true,
    "name": "adc-platform-monorepo",
    "scripts": {
        "build": "echo 'Building Web App...' && cd apps/web && npm install && npm run build"
    },
    "engines": {
        "node": "20.x"
    },
    "devDependencies": {
        "next": "14.2.3"
    }
}
```